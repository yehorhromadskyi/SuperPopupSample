<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SuperPopupSample"
             x:Class="SuperPopupSample.MainPage">
    <Grid>
        <StackLayout Orientation="Horizontal"
                     VerticalOptions="Start">
            <Button Text="Show Popup1"
                    HeightRequest="80"
                    HorizontalOptions="Start"
                    local:Gestures.TappedCommand="{Binding ShowPopup1Command}"/>

            <Button Text="Show Popup2"
                    HeightRequest="80"
                    HorizontalOptions="EndAndExpand"
                    local:Gestures.TappedCommand="{Binding ShowPopup2Command}"/>
        </StackLayout>

        <local:SuperPopup x:Name="PopupView1">
            <Frame HasShadow="True"
                   BackgroundColor="White">
                <Label Text="Popup 1"/>
            </Frame>
        </local:SuperPopup>

        <local:SuperPopup x:Name="PopupView2">
            <Frame HasShadow="True"
                   BackgroundColor="White">
                <Label Text="Popup 2"/>
            </Frame>
        </local:SuperPopup>
    </Grid>
</ContentPage>